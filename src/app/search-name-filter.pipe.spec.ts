import { SearchNameFilterPipe } from './search-name-filter.pipe';

describe('SearchNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
